@model IEnumerable<FrontEnd.Models.Sport>

@{
    ViewData["Title"] = "GetSport";
}


<style>



    body {
        background-image: url("https://images.unsplash.com/photo-1542020609584-35ba2e61d093?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=870&q=80");
        background-repeat: no-repeat;
        background-position-x: center;
        background-position-y: center;
        background-size: cover;
    }
</style>
<h1>List Of Sports</h1>

@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@
<center>
    <table class="table">

        <thead>

            <tr>
                @*<th>
                        @Html.DisplayNameFor(model => model.SportId)
                    </th>*@
                <th> @Html.DisplayNameFor(model => model.SportName)</th>
            </tr>

        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    @*<td>
                            @Html.DisplayFor(modelItem => item.SportId)
                        </td>*@
                    <td>
                        @Html.DisplayFor(modelItem => item.SportName)
                    </td>
                    @*<td>
                            <a asp-action="Edit" asp-route-id="@item.SportId">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.SportId">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.SportId">Delete</a>
                        </td>*@
                </tr>
            }
        </tbody>
    </table>
    @*@Html.ActionLink("Back", "Admin", "Home", null, new { @class = "btn btn-primary btn-large" })*@
</center>
@*<h1>GetSport</h1>

    <h2>
        <a asp-action="Create">Create New</a>
    </h2>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.SportName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.SportName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.SportId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.SportId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.SportId">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@
